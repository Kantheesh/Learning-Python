'''
14. Define a dict as below. Accept a input from the keyboard & display
'''

'''
Dictionary:
	Dictionaryis a key-value pairs
	keys:
		keys should be unique
		keys can be any immutable objects
	values:
		value can be anything
	It is non-indexable--> No order
	It is mutable
	It is iterable --> only keys will be considered
	ex:
		data = {1:"abc", "a":12345, 'RN1':"abc123"}
'''

data = {}

print("enter dictionary data(odd places for keys & even places for values):")
input(data)
for i in input(data)
	print(data)